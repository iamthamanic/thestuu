/**
 * Piano roll tool ids and defaults (FL-style).
 * Location: apps/dashboard/lib/piano-roll-tools.js
 */

export const PIANO_ROLL_TOOLS = {
  DRAW: 'draw',
  PAINT: 'paint',
  SELECT: 'select',
  ERASE: 'erase',
};

export const DEFAULT_PIANO_ROLL_TOOL = PIANO_ROLL_TOOLS.DRAW;
