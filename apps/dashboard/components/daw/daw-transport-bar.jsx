'use client';

/**
 * Transport control strip extracted from stuu-shell for incremental shell split.
 * Location: apps/dashboard/components/daw — re-exports transport UI props from parent until full extraction.
 */

import { memo } from 'react';

/**
 * Placeholder wrapper: transport controls remain in stuu-shell until Phase D continues.
 * This module establishes the daw/ folder structure for mix-panel and arrangement-panel splits.
 */
function DawTransportBarPlaceholder() {
  return null;
}

export default memo(DawTransportBarPlaceholder);
