{
  "name": "@thestuu/desktop",
  "private": true,
  "version": "0.1.0",
  "description": "TheStuu Tauri desktop shell — window and process lifecycle only; does not own DAW state",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
