{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TheStuu desktop shell — core webview only (no DAW IPC)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
